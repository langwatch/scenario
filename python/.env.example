OPENAI_API_KEY=
LANGWATCH_ENDPOINT=http://app.langwatch.ai
LANGWATCH_API_KEY="your-api-key"

# Voice integration tests (issue #350)
# Twilio — required for TwilioAgentAdapter integration tests
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
# ElevenLabs — required for ElevenLabsAgentAdapter (hosted) and ElevenLabsSTTProvider
ELEVENLABS_API_KEY=
# ElevenLabs hosted agent ID — created by `scripts/provision_elevenlabs_agent.py`
# Run: uv run python scripts/provision_elevenlabs_agent.py
ELEVENLABS_AGENT_ID=
# Gemini — required for GeminiLiveAgentAdapter
GEMINI_API_KEY=
# Voice @e2e gate — set to 1 to run @e2e tests locally (requires live infra)
# Use `make voice-demos-up` first to bring up local bots.
VOICE_E2E=
# Override the default Pipecat bot WebSocket URL (default: ws://localhost:8765/stream)
PIPECAT_BOT_URL=ws://localhost:8765/stream
