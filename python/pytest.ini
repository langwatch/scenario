[pytest]
asyncio_default_fixture_loop_scope=function
# Fail any individual test hanging longer than 120s. CI previously hung
# indefinitely in the "Run tests" step on one of the new voice unit
# tests; with this timeout a hung test fails fast with a traceback
# instead of killing the runner. Provided by pytest-timeout dep group.
timeout = 120
