---
title: Custom Observability - Control OpenTelemetry Tracing
description: Configure OpenTelemetry tracing in @langwatch/scenario. Filter spans with scenarioOnly and withCustomScopes presets, use setupScenarioTracing for explicit control, and integrate with existing OTel providers like @vercel/otel.
---

import { Callout } from "vocs/components";

# Custom Observability [Control which spans Scenario exports]

By default, Scenario lazily initializes OpenTelemetry tracing on the first `run()` call and auto-instruments your process. This works well when running scenarios in isolation (e.g., via `vitest`), but can cause problems when scenarios run inside a production server.

## Why This Matters

If you run scenarios inside a long-lived process -- for example, a Next.js server with Inngest cron jobs that periodically test your agents -- the default auto-instrumentation will capture **every** span in that process: HTTP requests, middleware, database queries, and more. All of those unrelated spans get exported alongside your scenario traces, adding noise and cost.

The `observability` config key gives you control over:

- **Which spans are exported** via filter presets
- **Which instrumentations are active** (or disabled entirely)
- **When tracing initializes** via `setupScenarioTracing()`

---

## Scenario-Only Spans

The simplest filter. Use `scenarioOnly` to export only spans created by `@langwatch/scenario`, discarding everything else.

This is the right choice when you want clean scenario traces without any server noise.

```typescript
// scenario.config.js
import { defineConfig, scenarioOnly } from "@langwatch/scenario";
import { LangWatchTraceExporter } from "langwatch/observability";

export default defineConfig({
  observability: {
    traceExporter: new LangWatchTraceExporter({
      filters: scenarioOnly,
    }),
    instrumentations: [], // disable auto-instrumentation
  },
});
```

Setting `instrumentations: []` prevents Scenario from registering any OpenTelemetry instrumentations (HTTP, fetch, etc.), so only the spans Scenario itself creates are recorded.

---

## Scenario + Custom-Tagged Spans

Sometimes you want to see more than just scenario spans -- for example, database queries or custom service calls that your agent makes. The `withCustomScopes()` filter includes `@langwatch/scenario` spans plus any additional instrumentation scopes you specify.

### Step 1: Configure the filter

```typescript
// scenario.config.js
import { defineConfig, withCustomScopes } from "@langwatch/scenario";
import { LangWatchTraceExporter } from "langwatch/observability";

export default defineConfig({
  observability: {
    traceExporter: new LangWatchTraceExporter({
      filters: withCustomScopes("my-app/database", "my-app/agent"),
    }),
    instrumentations: [], // disable auto-instrumentation
  },
});
```

### Step 2: Tag your spans

In your application code, create a tracer with the matching scope name and wrap the operations you want to capture:

```typescript
import { trace } from "@opentelemetry/api";

const tracer = trace.getTracer("my-app/database");

async function queryDatabase(sql: string) {
  return tracer.startActiveSpan("db.query", async (span) => {
    try {
      const result = await db.execute(sql);
      return result;
    } finally {
      span.end();
    }
  });
}
```

Only spans from `@langwatch/scenario`, `my-app/database`, and `my-app/agent` will be exported. Everything else (HTTP middleware, framework internals, etc.) is filtered out.

---

## Explicit Initialization with `setupScenarioTracing()`

By default, tracing initializes lazily on the first `run()` call. If you need tracing active before that -- for example, to capture spans during test setup or agent warm-up -- call `setupScenarioTracing()` explicitly.

```typescript
import { setupScenarioTracing } from "@langwatch/scenario";

// Initialize tracing before any run() call
setupScenarioTracing({
  instrumentations: [], // disable auto-instrumentation
});
```

`setupScenarioTracing()` accepts the same options as the `observability` config key. Once called, subsequent `run()` calls skip their own initialization.

<Callout type="info">
  If you configure observability in both `scenario.config.js` and via `setupScenarioTracing()`, the explicit call wins -- `run()` will not re-initialize tracing.
</Callout>

---

## Pre-Existing OpenTelemetry Provider

If your application already initializes OpenTelemetry -- for example, via `@vercel/otel`, Datadog, or your own `NodeTracerProvider` -- Scenario detects this automatically and adds its span processors to the existing provider. No configuration needed.

```
Your app initializes OTel (e.g., @vercel/otel)
         |
         v
  Scenario run() is called
         |
         v
  Scenario detects existing provider
         |
         v
  Adds judgeSpanCollector + LangWatch exporter
  to the existing provider
         |
         v
  Your existing spans continue flowing
  to your existing exporters, while
  scenario spans also go to LangWatch
```

This means you can add `@langwatch/scenario` to a project that already has observability set up without any configuration changes. Scenario will not overwrite or interfere with your existing tracing setup.

<Callout type="tip">
  When a pre-existing provider is detected, the `observability` config in `scenario.config.js` is ignored -- Scenario only attaches its processors to the provider that is already running.
</Callout>

---

## Configuration Reference

The `observability` key in `scenario.config.js` accepts a subset of `SetupObservabilityOptions` from the `langwatch` SDK:

| Option               | Type                        | Description                                                                                   |
| -------------------- | --------------------------- | --------------------------------------------------------------------------------------------- |
| `traceExporter`      | `SpanExporter`              | Custom span exporter. Use `LangWatchTraceExporter` with `filters` for span filtering.         |
| `instrumentations`   | `Instrumentation[]`         | OpenTelemetry instrumentations to register. Set to `[]` to disable auto-instrumentation.      |
| `spanProcessors`     | `SpanProcessor[]`           | Additional span processors. The internal `judgeSpanCollector` is always added automatically.  |
| `langwatch`          | `{ apiKey, endpoint }`      | Override LangWatch connection settings. Falls back to environment variables if not set.        |

---

## Next Steps

- [Configuration](/basics/configuration) -- Environment variables and config file reference
- [Custom Clients](/advanced/custom-clients) -- Configure custom LLM clients and parameters
- [Simulations Visualizer](/visualizations) -- View scenario traces in the LangWatch dashboard
